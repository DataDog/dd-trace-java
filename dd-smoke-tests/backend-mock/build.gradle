apply from: "$rootDir/gradle/java.gradle"
description = 'Mock Datadog backend used by smoke tests.'

dependencies {
  api project(':dd-smoke-tests')
  api testFixtures(project(':products:ci-visibility:ci-visibility-agent'))
}
