apply from: "$rootDir/gradle/java.gradle"

dependencies {
  api libs.groovy
  api libs.bundles.spock

  api libs.bytebuddy
  api libs.bytebuddyagent

  api group: 'uk.org.webcompere', name: 'system-stubs-jupiter', version: '1.2.1' // Branch 1.x for Java 8.x compatibility
  api group: 'commons-fileupload', name: 'commons-fileupload', version: '1.5'
}
