apply from: "$rootDir/gradle/java.gradle"

muzzle {
  pass {
    group = 'junit'
    module = 'junit'
    versions = '[4.7,)'
  }
}

dependencies {
  compileOnly group: 'junit', name: 'junit', version: '4.7'
}
