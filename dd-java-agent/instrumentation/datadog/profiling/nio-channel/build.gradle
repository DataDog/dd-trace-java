apply from: "$rootDir/gradle/java.gradle"

// No muzzle block: NIO channels (ServerSocketChannel, SocketChannel) exist since JDK 1.4
// and there is no minimum JDK version constraint for this module.

dependencies {
  testImplementation libs.bundles.junit5
  testImplementation libs.bundles.mockito
  testImplementation libs.bytebuddy
}
