// This won't work until the akka and scala integrations are split into separate projects.
//muzzle {
//  pass {
//    coreJdk()
//  }
//}

apply from: "$rootDir/gradle/java.gradle"

apply plugin: 'org.unbroken-dome.test-sets'

testSets {
  testJava8
}

dependencies {
  testCompile project(':dd-java-agent:instrumentation:trace-annotation')

  testJava8Compile project(':dd-java-agent:instrumentation:java-concurrent')
  testJava8Compile project(':dd-java-agent:instrumentation:trace-annotation')
}

// Run Slick library tests along with the rest of unit tests
test.finalizedBy testJava8
