
muzzle {
  pass {
    group = 'org.springframework.boot'
    module = 'spring-boot'
    versions = "[1.3.0.RELEASE,3)"
  }
  pass {
    group = 'org.springframework.boot'
    module = 'spring-boot'
    versions = "[3,)"
    javaVersion = "17"
  }
}

apply from: "$rootDir/gradle/java.gradle"

dependencies {
  compileOnly group: 'org.springframework.boot', name: 'spring-boot', version: '1.0.0.RELEASE'
}
