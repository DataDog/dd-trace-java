type Query {
  bookById(id: ID): Book
}

#TODO test mutation
#type Mutation {
#  addBook(id: ID, name: String, author: ID): Book
#}

type Book {
  id: ID
  name: String
  pageCount: Int
  author: Author
}

type Author {
  id: ID
  firstName: String
  lastName: String
}
