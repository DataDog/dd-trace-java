package example

import example.Person

declare GeneratedFact
    value : String
end

rule "Mark person as adult"
when
    $p : Person(age >= 18, adult == false)
then
    $p.setAdult(true);
    System.out.println("Rule fired for " + $p.getName());
    update($p);
end
