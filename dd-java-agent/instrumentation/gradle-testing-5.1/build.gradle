plugins {
  id 'dd-trace-java.module.instrumentation'
}

repositories {
  maven {
    url = "https://repo.gradle.org/artifactory/libs-releases-local"
  }
}

// gradle >= v5.1
dependencies {
  compileOnly gradleApi()
  compileOnly project(":dd-java-agent:instrumentation:junit:junit-4:junit-4.10")
}
