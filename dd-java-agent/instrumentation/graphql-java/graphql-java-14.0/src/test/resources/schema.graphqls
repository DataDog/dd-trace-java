type Query {
  bookById(id: ID): Book
}

#TODO test mutation
#type Mutation {
#  addBook(id: ID, name: String, author: ID): Book
#}

type Book {
  id: ID
  name: String
  pageCount: Int
  author: Author
  cover: String
  asyncCover: String
  isbn: ID!
  bookHash: Int!
  year: Int
}

type Author {
  id: ID
  firstName: String
  lastName: String
}
